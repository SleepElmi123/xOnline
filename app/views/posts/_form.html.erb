<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Title %><br>
    <%= f.text_field :CodeName %>
  </div>
  <div class="field"><br>
    <h4><%= f.label :CEO %></h4><br>

    <%= f.check_box :CEO %>
     <h5>Check Box For Yes</h5>
  </div>
  <div class="field">
    <%= f.label :Full_Story %><br>
    <%= f.text_area :Story %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
